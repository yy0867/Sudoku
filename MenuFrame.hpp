#ifndef __MENU_FRAME_HPP__
#define __MENU_FRAME_HPP__

#include "Frame.hpp"
#include <vector>

void printMenu(int xOffset, int yOffset, int width, int height,
    int color, int term, vector<string> menuList);

#endif
